// use stm32f1xx_hal::gpio::{Output, Pin};

// /*
// pub type PinType = Pin<'C', 13, Output>;

// */

// pub fn led_blink<const PORT: char, const N: u8>(
    // pin: &mut Pin<PORT, N, Output>,
    // delay_cicles:u32,
// ) {
        // pin.set_low();
        // cortex_m::asm::delay(delay_cicles);
        // pin.set_high();
        // cortex_m::asm::delay(delay_cicles);
// }